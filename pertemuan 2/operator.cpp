#include <iostream>
using namespace std;

int main() {
    int a, b;

    system("cls");

    //Assignment Operator, "=", ngasih nilai ke dalam suatu variabel.
    a = 3;
    b = 5;

    // //Arithmetical Operator, operator untuk operasi matematika.
    // int tambah = a + b;
    // int kurang = a - b;
    // int kali = a * b;
    // // float bagi = a / b;
    // float bagi = (float)a/ (float)b; // type casting, mengubah type data suatu variabel menjadi tipe data lain secara paksa.
    // int modulo = a % b;

    // cout << "Hasil Penjumlahan : " << tambah << endl;
    // cout << "Hasil Pengurangan : " << kurang << endl;
    // cout << "Hasil Perkalian : " << kali << endl;
    // cout << "Hasil Pembagian : " << bagi << endl;
    // cout << "Hasil Sisabagi : " << modulo << endl;

    // //Relational Operator, bandingin nilai dari operand. Hasil berupa boolean 0 1. (nilainya yang di bandingkan)
    // cout << (a == b) << endl;
    // cout << (a < b) << endl;
    // cout << (a != b) << endl;

    //Logical Operator (&&, ||, !), bandingkan dua nilai variabel yang bertipe boolean (bolean nya yang di bandingkan)
    // cout << (true && true) << endl;
    // cout << (true && false) << endl;

    // cout << (true || false) << endl;
    // cout << (false || false) << endl;

    // cout << !true << endl;
    // cout << !false << endl;

    // //Bitwise Operator ---> biner (&, |, ^, ~, >>, <<)
    // cout << (5 & 7) << endl;
    // cout << (5 | 7) << endl;
    // cout << (5 ^ 7) << endl;   
    // cout << (~7) << endl;
    // cout << (7 << 2) << endl;
    // cout << (7 >> 2) << endl;

    // // Operator Kondisional (?), Ternary, 3 operand memerlukan kondisi --> hasil 1 ? hasil 2
    // int max;
    // max = (a > b) ? a : b;
    // cout << "Nilai terbesar : " << max << endl; //5

    // // Shorthand
    // a += 7; // a = a + 7
    // cout << a << endl;

    // // Increment, Decrement
    // // Pre Increment, ++ ada di depan variabel
    // cout << a << endl; //3
    // cout << ++a << endl; // ++3 = 4 ( langsung )

    // Post Increment, ++ ada di belakang variabel
    // cout << a << endl; // 3
    // cout << a++ << endl; // 3 + 1 (harus di panggil lagi, nampilin baru ngerjain)
    // cout << a << endl;

    // // Pre Decrement, -- ada di depan variabel
    // cout << b << endl; // 5
    // cout << --b << endl;  // --5 = 4

    // // Post Decrement, -- ada di belakang variabel
    // cout << b << endl; // 5
    // cout << b-- << endl; // b-- = 5 (menghasilkan 4 harus di panggil lagi)
    // cout << b << endl;

}   